#include "NumClass.h"
#include <stdlib.h>
#include <stdio.h>

void main()
{
    char c[1000];
    FILE* in_file  = fopen("./self_check_hw1/inputs", "r"); // read only 
    // if(in_file == NULL)
    // fscanf(in_file, "%[^\n]", c);
    // printf("Data from the file:\n%s", c);

    // printf(in_file);
}